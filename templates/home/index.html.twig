{% extends 'base.html.twig' %}

{% block title %}Wish list{% endblock %}

{% block body %}
    <div>
        {% if app.user %}
            <h5 class="text-center">Welcome {{ app.user.email }}</h5>
            {% else %}
                <h5  class="text-center"><a href="{{ path('register') }}" class="text-dark">Joint us</a></h5>
                <h5  class="text-center">or</h5>
                <h5  class="text-center"><a href="{{ path('login') }}" class="text-dark">Log in</a></h5>
        {% endif %}
    </div>
{% endblock %}
